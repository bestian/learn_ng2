/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SlashPipe } from './slash.pipe';

describe('Pipe: Slash', () => {
  it('create an instance', () => {
    let pipe = new SlashPipe();
    expect(pipe).toBeTruthy();
  });
});
